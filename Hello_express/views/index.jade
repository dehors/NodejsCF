extends ./Layout/master.jade
block contenido
    h1 Bienvenido a FotoFacilito
    a(href="/login") Login
    br
    a(href="/singup") Singup

    for  imagen in imagenes
            h2=imagen.title
            if(imagen.extension)
                img(src="/static/image/#{imagen._id}.#{imagen.extension}" style="max-width:100%;height: 70px;width: 100px;")
            if(typeof imagen.creator != "undefined")
                p Por #{imagen.creator.email}
//- html
//-   head
//-     title!= title
//-   body
//-     // this is a comment
//-     h1!= message
//-     -var array = [1,2,3,4,5];
//-     if true
        
//-       -if(true)
//-         -for (var i = array.length - 1; i >= 0; i--)
//-           p= array[i]
//-           p!= "<h1>"+array[i]+"</h1>"
//-           p Hello #{array[i]+1}
//-           p.
//-             Hello click on this #[a(href="facebook.com") link] please

//- html
//- 	head
//- 		title My first page with express
//- 	body
//- 		form(action="/", method="POST")
//- 			input(type="submit",value="Enviar")